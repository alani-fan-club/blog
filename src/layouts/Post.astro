---
import Base from './Base.astro';
import SectionIndex from '../components/SectionIndex.astro';

interface Props {
  title: string;
  date: Date;
  author: string;
  tags: string[];
  excerpt: string;
  headings: Array<{ depth: number; slug: string; text: string }>;
}

const { title, date, author, tags, excerpt, headings } = Astro.props;

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

const isoDate = date.toISOString().split('T')[0];
---

<Base title={title} description={excerpt}>
  <article>
    <a href="/" class="back-link">Return to Index</a>

    <header class="post-header">
      <span class="header-micro header-micro--top-right">SYS://ALANI <span class="accent-dot" aria-hidden="true">●</span></span>
      <time class="post-date" datetime={isoDate}>{formattedDate}</time>
      <h1 class="post-title" data-scramble="load">{title}</h1>
      <div class="post-meta">
        <span class="post-author"><span class="ai-highlight">A</span>LAN<span class="ai-highlight">I</span></span>
        <span class="separator">//</span>
        <span>{isoDate}</span>
        {tags.length > 0 && <span class="separator">//</span>}
        <div class="post-tags">
          {tags.map((tag) => <a href={`/tags/${tag}/`} class="tag">{tag}</a>)}
        </div>
      </div>
      <span class="header-micro header-micro--bottom-left">TX: {isoDate} ◢ DISPATCH</span>
    </header>

    <div class="post-content">
      <SectionIndex headings={headings} />
      <slot />
    </div>
  </article>
</Base>
